node_modules/
dist/
.cache/
*.tsbuildinfo
.DS_Store
.env
.env.*
!.env.example

# Your real config — copy site.config.example.ts to site.config.ts and edit.
# Kept out of git so personal details aren't committed to the template.
site.config.ts
site.config.local.ts

# Working notes and scratch files that are about this deployment rather than
# the template.
findings/
download.html

# Anything belonging to your instance rather than the template: app icons,
# product artwork, logos, screenshots. This repository is a public template,
# so brand assets you own must stay out of it. Reference them from
# site.config.ts as /instance/<file>.
public/instance/
