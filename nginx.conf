server {
    listen 60610;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # the container listens on an internal port behind a proxy, so a directory
    # redirect must stay relative or it sends the browser to host:60610
    absolute_redirect off;

    location / {
        try_files $uri $uri/ /index.html;

        # the html names content-hashed chunks, and a deploy deletes the old
        # ones. cached html therefore asks for files that are gone, so it must
        # be revalidated on every navigation. the hashed assets below stay
        # immutable, which is what makes that cheap.
        add_header Cache-Control "no-cache" always;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|woff|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location ~* \.(svg|webmanifest)$ {
        expires 1h;
        add_header Cache-Control "public";
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml;
}
