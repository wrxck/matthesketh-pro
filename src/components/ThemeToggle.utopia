<template>
  <div class="theme-toggle" role="group" aria-label="Theme">
    <button
      type="button"
      class="tt-btn"
      :class="themePref() === 'light' ? 'tt-btn active' : 'tt-btn'"
      :aria-pressed="themePref() === 'light' ? 'true' : 'false'"
      @click="() => setTheme('light')"
      aria-label="Light theme"
      title="Light"
    >
      <svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M4.9 4.9l1.4 1.4M17.7 17.7l1.4 1.4M2 12h2M20 12h2M4.9 19.1l1.4-1.4M17.7 6.3l1.4-1.4"/></svg>
    </button>
    <button
      type="button"
      class="tt-btn"
      :class="themePref() === 'auto' ? 'tt-btn active' : 'tt-btn'"
      :aria-pressed="themePref() === 'auto' ? 'true' : 'false'"
      @click="() => setTheme('auto')"
      aria-label="System theme"
      title="Auto (system)"
    >
      <svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2"/><path d="M8 21h8M12 17v4"/></svg>
    </button>
    <button
      type="button"
      class="tt-btn"
      :class="themePref() === 'dark' ? 'tt-btn active' : 'tt-btn'"
      :aria-pressed="themePref() === 'dark' ? 'true' : 'false'"
      @click="() => setTheme('dark')"
      aria-label="Dark theme"
      title="Dark"
    >
      <svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12.8A9 9 0 1 1 11.2 3 7 7 0 0 0 21 12.8z"/></svg>
    </button>
  </div>
</template>

<script>
import { themePref, setTheme } from '../lib/theme'
</script>

<style scoped>
.theme-toggle {
  position: fixed;
  top: 1rem;
  right: 1rem;
  z-index: 50;
  display: inline-flex;
  border: 1px solid var(--border);
  background: var(--bar-bg);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
}
.tt-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 34px;
  height: 30px;
  padding: 0;
  border: none;
  background: none;
  color: var(--text-faint);
  cursor: pointer;
  transition: color 0.15s ease, background-color 0.15s ease;
}
.tt-btn + .tt-btn {
  border-left: 1px solid var(--border);
}
.tt-btn:hover {
  color: var(--text);
}
.tt-btn.active {
  background: var(--text);
  color: var(--bg);
}
.tt-btn:focus-visible {
  outline: 2px solid #6b7cff;
  outline-offset: -2px;
}
.tt-btn svg {
  width: 15px;
  height: 15px;
  pointer-events: none;
}

@media (max-width: 480px) {
  .theme-toggle { top: 0.7rem; right: 0.7rem; }
  .tt-btn { width: 31px; height: 28px; }
  .tt-btn svg { width: 14px; height: 14px; }
}

@media (prefers-reduced-motion: reduce) {
  .tt-btn { transition: none; }
}
</style>
